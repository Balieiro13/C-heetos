#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;
    int idler[2][n], i = 0, j = 0;
    while (i < n){
        cin >> idler[0][i];
        i++;
    }
    while (j < j){
        cin >> idler[1][j];
        j++;
    }
}